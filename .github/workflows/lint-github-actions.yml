name: "Lint GitHub Actions"

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev
    paths:
      - ".github/workflows/**"

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: reviewdog/action-actionlint@f54359bbad3ecf7b660a74e04576e6fc58864861 # tag=v1.28.0
        with:
          reporter: github-check
          fail_on_error: true
