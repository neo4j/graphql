mutation SimpleDelete {
    deleteMovies {
        nodesDeleted
    }
}

mutation NestedDeleteInUpdate {
    updateMovies(update: { actors: { delete: { where: { node: { name_CONTAINS: "Shark" } } } } }) {
        movies {
            title
        }
    }
}
