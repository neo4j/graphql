query SimpleQuery {
    movies {
        title
        actors {
            name
        }
    }
}

query SimpleQueryWithNestedWhere {
    movies(where: { actors_SOME: { name: "Keanu Reeves" } }) {
        title
        actors {
            name
        }
    }
}
