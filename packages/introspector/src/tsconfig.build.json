{
    "extends": "../../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "../dist"
    },
    "exclude": ["**/*.test.ts"],
    "references": [
        { "path": "../" } // for package.json import
    ]
}
